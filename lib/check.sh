#!/usr/bin/env bash
### ============================================================
# VpsScriptKit - 通用检测工具 - 函数库
#
# 职责：
# - 只提供“判断”，不做任何操作
### ============================================================

# ------------------------------
# 判断当前用户是否为 root
# ------------------------------
is_root() {
  # id -u 返回当前用户 UID
  # root 的 UID 固定为 0
  [[ "$(id -u)" -eq 0 ]]
}
